# 网页桌面 · 最小设计系统

## 原则

1. 颜色和字体优先使用 Obsidian 语义变量，保证明暗主题与社区主题兼容。
2. `--wd-*` 只表达插件内重复出现的视觉语义，不复制一套独立色板。
3. 原生 `Menu`、`Modal`、`Setting` 和 HTML 按钮优先；自由画布对象才使用自定义组件样式。
4. 新视觉值出现三次以上，或已承担稳定语义时，才提升为 token。
5. 不引入独立组件库：Obsidian 已提供菜单、弹窗和主题语义；插件只维护画布特有的交互原语。

## 表面层级（2026-09-03 起）

画布上的东西分三层，每层只能用自己那一档的边、阴影和圆角；"所有对象都是一张带阴影的卡片"正是此前不精致的根因。

| 层 | 成员 | 边 | 阴影 | 圆角 |
|---|---|---|---|---|
| L0 画布 | 点阵背景 | 无 | 无 | 无 |
| L1 内容对象 | 图标瓦片、预览卡、图片、文件卡、待导入卡 | 1px `--wd-border-color` | `--wd-shadow-object`（几乎不可见）；hover 才升到 `--wd-shadow-hover` | `--wd-radius-md`（图标瓦片 `lg`，图片/文本 `sm`） |
| L2 浮层 | 选中态工具栏、创建栏、缩放条、菜单、弹窗 | 1px | `--wd-shadow-float` + 轻微毛玻璃 | `--wd-radius-md` |
| 墨 | 区域、文本框、评分、箭头、caption | 默认无边无底；`has-border` 为 1px 实线，`has-fill` 为 8–10% 底色 | 无 | `sm`/`md` |

- 选中态统一是 `--wd-selection-ring`（2px accent），不叠加大阴影；hover 只提升边框色与一点阴影。
- 虚线只保留给手势反馈（框选、缩放中的临时环）；长期存在的对象不用虚线。
- 缩放手柄统一是 10px 圆点（accent 底、背景色描边），静止隐藏，hover / 选中淡入。
- Obsidian 给 `button:not(.clickable-icon)` 加了内阴影与底色，优先级高于单个类名；画布内所有工具按钮由 `button.web-desk-*` 选择器统一压平，工具条才是一个整体。
- 站点图标先显示首字母色块，`FaviconResolver` 取到 ≥24px 的图标后再替换；图标缓存在 `<imageFolder>/网站图标/`。
- 语义缩放：根节点带 `data-zoom-band`（near ≥ 0.75、mid ≥ 0.45、far）。mid 隐藏 caption、标题一行；far 再隐藏图标标题与摘要。区域名按 `1/var(--wd-zoom)` 反向缩放，缩小视图靠区域名导航。

## Tokens

| Token | 用途 |
|---|---|
| `--wd-surface` / `--wd-surface-float` | L1 内容对象表面 / L2 浮层表面 |
| `--wd-border-color` / `--wd-border-strong` | 常规边框与点阵颜色 / hover 时加深的边框 |
| `--wd-radius-sm/md/lg` | 6/10/12px：墨与图片 / 卡片与浮层 / 图标瓦片 |
| `--wd-space-1/2/3` | 4/8/12px 重复间距 |
| `--wd-control-compact` | 画布工具栏的 32px 桌面控件尺寸 |
| `--wd-shadow-object/hover/float` | L1 静止 / L1 hover / L2 浮层阴影（`--wd-shadow-sm/md` 是旧别名） |
| `--wd-selection-ring` | 选中与键盘焦点共用的 2px accent 描边 |
| `--wd-motion-fast` | hover 与边框反馈动画 |
| `--wd-rating-active` | 评分星级的语义强调色，优先继承主题黄色 |

## 组件约束

- `web-desk-toolbar`：只放视图级短操作，按钮统一使用 `web-desk-tool-btn`；画布浮动控件使用 32px 紧凑桌面规格。
- `web-desk-create-rail`：两种画布共享的可见创建入口，与 Obsidian Canvas 一样放在底部居中；一级只保留添加、文本、分组、更多四项，细分动作进入原生菜单；按钮与对象工具栏共享紧凑尺寸和 16px 图标。新建文本框按矩形找最近空位并自动选中。
- `web-desk-icon`：网页收藏；视觉重心是方形缩略图和两行标题。
- `web-desk-rating`：1–5 星评分，160×56 的一行星星，不是卡片；独立评分只有星星，绑定评分在星星上方显示目标名，链接失效时保留评分并使用 missing 状态。
- `web-desk-image`：Vault 图片附件；拖动主体，右下角手柄等比例缩放。
- `web-desk-textbox`：自由备注；允许非等比例调整。默认只显示文字，边框和底色是显式可选外观。
- `web-desk-group`：归类范围；名称挂在框外上沿并保持屏幕尺寸，边框和底色是显式可选外观（实线、淡底），不承载正文内容。
- `web-desk-color-picker`：区域与文本框共用的颜色入口；七个稳定预设负责跨画布一致性，系统取色器与合法 HEX 输入负责完全自定义。自定义值只接受 `#RGB` / `#RRGGBB`，持久化前统一为六位小写。
- `*-resize`：右下角统一的圆点缩放手柄；静止时隐藏，元素 hover 或键盘聚焦时淡入，缩放会话中保持可见；不得各组件自行复制尺寸和边框规则。

## 交互原语

### 双画布能力合同

- 主收藏画布与文内 `web-desk` 画布共享同一套用户能力：URL/文本/图片/Markdown 导入，网页、文件、图片、文本、评分、空间区域、箭头与逻辑组合，以及选择、移动、缩放、吸附、平移、缩放和适应内容。
- URL 导入只能调用 `importUrlAsBookmark`：先按 URL 复用已有收藏 Markdown，再抓取并落盘；文内网页用 `bookmarkPath` 关联收藏文件，普通 Markdown 文件卡片继续使用 `path`，两者不得混用。
- 网页对象的展示、属性、菜单和悬浮工具条优先调用共享原语；新增用户能力必须同时接入两种画布，并更新 `canvas-contracts.test.cjs` 的 parity 门禁。
- 允许不同的只有宿主适配：主画布布局写入收藏 Markdown frontmatter，文内画布布局写回代码块；文内画布可调整块高度，主画布填满 Obsidian 视图。宿主差异不得改变对象能力。
- 文内画布的“全屏编辑”只切换同一实例的宿主展示状态；不得复制画布或建立第二份数据。进入和退出保留缩放、平移、选择与块高度，`Esc` 退出，写回导致代码块重渲染时短暂交接全屏状态。
- `Delete` / `Backspace` 的画布语义是“移出当前画布”：网页 Markdown、原笔记和图片附件必须保留；多选按一次批量移除，相关箭头同步清理。只有显式右键“删除收藏文件…”才允许进入带确认的文件删除路径。输入框或 `contenteditable` 正在编辑时不得劫持删除键。
- 旧文内代码块没有 `bookmarkPath` 时保持可读，不静默联网或改写；用户首次选择“创建并关联 Markdown”后完成迁移。
- 含合法 `web-desk` 块的 Markdown 文件卡片自动升级为画布引用；引用只保存目标路径，不复制目标 JSON。
- 点击画布引用在当前画布原位下钻，顶部面包屑和 `Esc` 提供对称返回路径；每次只渲染当前层，循环引用必须拒绝，目标缺失或坏数据不得生成空画布覆盖原文。
- 主画布与文内画布使用同一画布引用识别、创建和下钻协议；普通 Markdown 文件卡片继续按原方式打开笔记。

- `beginCanvasPointerSession`：所有对象拖拽和缩放的唯一指针生命周期。移动与提交监听挂在稳定 `document`，统一处理缩放换算、移动阈值、取消和 `is-resizing` 清理。
- `CanvasSnapSession`：拖动与缩放共用的几何吸附协议。网格与对齐阈值按屏幕像素换算；边缘/中心对齐优先于 24px 网格；吸附与释放使用不同阈值以消除边界抖动。手势中的几何保留小数以避免高倍缩放下跳动，只在提交持久化时取整。
- `CanvasSnapGuideLayer`：每块画布只维护横纵两条常驻 DOM 参考线，手势中复用、结束即隐藏，不在 `pointermove` 中反复分配节点或添加追赶指针的过渡动画。
- 可见点阵与几何网格是同一个 24px 坐标系；画布平移或缩放时，点阵的 `background-position` / `background-size` 必须同步更新，不能出现视觉网格与实际落点错位。
- `web-desk-object-selection`：多选和逻辑组合共用的选择框。几何永远由成员实时推导，不保存第二份边界数据。
- `KeyedSerialTaskQueue`：同一 Markdown 的 frontmatter 写入严格串行；不同文件仍可并发，避免旧的读-改-写晚到后覆盖新位置。
- `CardPropertiesModal`：网页名称、评分、备注属于网页对象自身；独立评分组件只用于与对象分离的评价表达。
- `web-card`：主画布与文内画布共享的网页视觉原语。同一网页实体只切换 placement 外观，不复制内容数据。
  - `icon`：默认紧凑入口，沿用独立的正方形 `size`。
  - `preview`：带封面、域名、标题、摘要和属性提示的可自由调整宽高卡片，使用独立 `previewWidth` / `previewHeight`。
    - `visual`：封面全幅、渐变承托标题，适合视觉识别。
    - `article`：封面与摘要上下分区，适合阅读判断，也是旧数据默认模板。
    - `compact`：缩略图与文字横排，适合高密度整理。
  - `preview` 在界面中称为“卡片”，封面优先保存为 Vault 内本地资产；没有可用封面时，桌面端生成网页截图。
  - `embed`：沙箱实时网页；仅允许 HTTPS，预检明确拒绝嵌入的站点并提供“恢复卡片”出口。iframe 不获得同源、表单、弹窗或下载权限。
  - 三种模式围绕对象中心切换；旧数据和非法模式必须回落到 `icon`。
  - 单击选择对象并显示上下文工具栏；双击或工具栏“打开”才跳转，避免选择与导航争抢同一手势。
  - `caption` 是网页对象的外显说明，和只在属性提示中展示的内部 `note` 分开保存。
  - 正文快照和 Stack 属于后续独立能力，不混入展示模式字段。
- `web-desk-selection-toolbar`：所有单选对象共用的悬浮工具栏。首段显示对象类型，主操作保持稳定，破坏性和低频动作收进原生“更多”菜单；主画布与文内画布必须调用同一构造器。
- `web-desk-pending-card`：链接导入和 Embed 预检的落点状态。加载、失败、重试与移除都在对象原位置表达；Notice 只用于完成回执，不承担过程状态。
- 空状态必须统计网页、文件、图片、文本、分组、箭头、评分和临时导入状态；任一可见对象存在时不得覆盖画布。

## 两种“分组”

- 空间区域 `web-desk-group`：可见的命名范围；元素中心进入即成为成员，移出即退出，移动区域会携带成员。旧数据字段仍使用 `group` / `desk_group`，避免破坏兼容。
- 逻辑组合 `objectGroup`：多选后显式创建，用于一起移动与缩放；它与空间区域互不替代。
- 逻辑组合 `desk_object_group`：Figma 式几何关系，没有常驻容器边框；成员一起选择、移动和缩放。

两者不得复用同一字段或同一视觉边缘。将来若增加可折叠 Stack，也必须作为第三种明确容器语义，而不是继续扩充分组框。

## 状态

- hover：只提升边框与阴影，不改变布局。
- selected：使用 Obsidian accent，不新增插件私有主色。
- missing：附件不存在时保留布局并显示占位，不自动删除数据。
- destructive：通过 Obsidian `Menu`/`Modal` 表达；移出图片只删画布引用，不删除附件。
